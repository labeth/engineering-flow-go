# /engflow.gate

Run the end-to-end gate in one command.

## Usage

`/engflow.gate <feature-id>`

## Behavior

Run:
`go run ./cmd/engflow gate --feature <feature-id>`

Returns verify, drift, and status results.

