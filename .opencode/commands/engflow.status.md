# /engflow.status

Show current flow status and next action.

## Usage

`/engflow.status <feature-id>`

## Behavior

Run:
`go run ./cmd/engflow status --feature <feature-id>`

Return the rendered status summary and next action.

