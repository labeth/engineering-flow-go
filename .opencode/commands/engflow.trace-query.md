# /engflow.trace-query

Search repository files for references to one REQ identifier.

## Usage

`/engflow.trace-query <req-id>`

## Behavior

Run:
`go run ./cmd/engflow trace-query --id <req-id>`

Returns path + line matches.

